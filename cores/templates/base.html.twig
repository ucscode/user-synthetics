{# The base template of user synthetics #}
<!doctype html>
<html>
<head>
    
    <title>{{ page_title|default('User Synthetics') }}</title>
    <script>const Uss = JSON.parse(atob('{{ Uss.jsElement }}'));</script>

    {% block head %}

        {% block head_metas %}
            <meta charset="utf-8">
            <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
            <meta name='description' content='{{ page_description|default('') }}'>
        {% endblock %}

        {% block head_css %}
            {{ Uss.renderBlocks('head_css', 1)|raw }} 
        {% endblock %}

        {% block head_js %}
            {{ Uss.renderBlocks('head_js', 1)|raw }}
        {% endblock %}

    {% endblock %}

</head>
<body>

    {% block body %}
        
        {{ Uss.renderBlocks('body_intro', 1)|raw }}

        {% block body_main %}{% endblock %}

        {% block body_js %}
            {{ Uss.renderBlocks('body_js', 1)|raw }}
        {% endblock %}

        {{ Uss.renderBlocks('body_outro', 1)|raw }}

    {% endblock body %}

</body>
</html>