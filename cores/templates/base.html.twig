<!doctype html>
<html lang="{{ html_language }}">
<head>
    
    <script>const Uss = JSON.parse(atob('{{ __uss.jsCollectionEncoded }}'));</script>
    
    <title>{{ page_title|default('User synthetics') }}</title>

    {% block head %}

        {% block head_meta %}
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1">
            <meta name='description' content='{{ page_description }}'>
            <link rel="icon" href="{{ favicon }}" type="image/x-icon">
        {% endblock %}

        {% block head_resource %}
            {{ __uss.app.renderBlocks('head_resource', _context)|raw }} 
        {% endblock %}

        {% block head_javascript %}
            {{ __uss.app.renderBlocks('head_javascript', _context)|raw }} 
        {% endblock %}

    {% endblock %}

</head>
<body>

    {% block body %}
        
        {% block body_main %}
            {{ __uss.app.renderBlocks('body_main', _context)|raw }}
        {% endblock %}

        {% block body_javascript %}
            {{ __uss.app.renderBlocks('body_javascript', _context)|raw }}
        {% endblock %}
        
    {% endblock body %}

</body>
</html>