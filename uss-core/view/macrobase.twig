{% macro renderExternalBlockContent(blockname) %}
    {% set block_elements = Uss.twigBlockManager.get(blockname) %}
    {% if  block_elements is not empty %}
        {% for content in block_elements %}
{{ content|raw }}
        {% endfor %}
    {% endif %}
{% endmacro %}